---
default: -p html -p bdd -p json -p junit
html: --format html --out=features.html
dot: --format progress
bdd: --format pretty
json: --format json -o "report.json"
junit: --format junit -o "prints"
